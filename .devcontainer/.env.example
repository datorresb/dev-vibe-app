# Copy this file to .env (which is gitignored) and fill in your credentials
# WARNING: These will be written in plaintext to ~/.git-credentials inside the dev container.
# Only use a PAT with the minimal required scopes.
#
# mv .devcontainer/.env.example .devcontainer/.env
# Then rebuild / reopen the dev container.
#
# Example minimal scopes for typical repo work: repo, workflow (if you trigger actions), read:packages.
# Do NOT store production secrets here.

# GitHub Credentials (if using GitHub)
GITHUB_USER=your_github_username
GITHUB_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional tooling installs (set to true to enable)
# These are read by .devcontainer/postCreate.sh via the --env-file in devcontainer.json.

# Install Claude Code CLI
INSTALL_CLAUDE=false

# Install Backlog.md CLI (npm global package: backlog.md)
INSTALL_BACKLOG_MD=false